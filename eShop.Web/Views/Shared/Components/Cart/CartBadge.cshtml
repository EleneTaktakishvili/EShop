@model int

    <a href="@Url.Action("Index", "Order")" class="btn btn-outline-dark">
        <i class="bi-cart-fill me-1"></i>
        კალათა
        <span class="badge bg-dark text-white ms-1 rounded-pill" id="cart-badge">@Model</span>
    </a>

